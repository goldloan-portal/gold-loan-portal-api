# AI Log — gold-loan-portal-api

Required by the assignment brief (see `PRODUCT.md` → Submission Deliverables). This log is updated per ticket as work lands (see `CLAUDE.md` → AI Workflow & Prompt Logging), not per message.

## AI tools used

- Claude Code (Sonnet 5), used interactively for the entirety of this repo's work so far.

## Required: 2 exact prompts (form state management or backend validation rules)

**1 of 2 captured**, from `GLA-11` — the first ticket to actually build backend validation rules (Zod schema + a `validate(schema)` middleware for `POST /api/v1/leads/submit`):

1. "Work on the story GLA-11 using the workflow and the worktree." (the ticket itself, fetched from Jira, specified the Zod validation, LTV/purity calculation, 7-day dedup, and Prisma `Lead` persistence requirements verbatim — this session worked from that ticket body, not a separately re-typed prompt).

Second exact prompt still pending — will be filled in from a later validation-bearing ticket (e.g. `GLA-15`'s `GET /api/v1/leads`, or the frontend form) rather than backfilled artificially here.

## Required: 1 instance of flawed AI-generated code, caught and fixed

**Pending.** One real near-miss so far, worth logging even though it isn't the required category: the initial `tsconfig.json` used `"moduleResolution": "node"`, which the installed TypeScript version (`^7.0.2` at the time) rejected outright (`TS5108: Option 'moduleResolution=node10' has been removed`) — caught immediately by running `tsc` rather than assuming the config was fine, fixed by switching to `"nodenext"` and later pinning TypeScript back to `~6.0.2` for `typescript-eslint` compatibility. `GLA-11` was audited closely for a genuine logic flaw (in particular, floating-point arithmetic in `calculatePureGoldWeight`/`calculateMaxEligibleLoan` before the result lands in a `Decimal` column) but the result checked out exactly against the assignment's worked example and realistic value ranges never approach the precision where float error would surface — so nothing qualifying found there either. Not an AI-authored logic bug, so it doesn't satisfy the requirement above — noted for completeness rather than fabricating one.

## Ticket Log

### Pre-ticket — Initial scaffolding

- Prompt: Initialize a backend Express application with TypeScript (and a companion React + Vitest frontend in the sibling repo), before any Jira tickets existed for this project.
- Key decisions: plain Express + TypeScript, `tsx` for dev, `cors`/`dotenv` as the only initial runtime dependencies, a single `GET /health` route to verify the setup end-to-end (build, start, curl).
- Files: `package.json`, `tsconfig.json`, `src/index.ts`, `.env`, `.env.example`, `.gitignore`.

### GLA-2 — Setup Husky + lint-staged + commit hooks

- Prompt: Set up Husky pre-commit hooks (lint-staged with ESLint `--fix` + Prettier, plus a whole-project typecheck) in both repos, following the acceptance criteria on the Jira ticket and the conventions found in a pair of reference production repos.
- Key decisions: switched the repo from npm to pnpm and from the Vite-scaffolded `oxlint` to ESLint, to match the reference repos' tooling — both confirmed with the user rather than assumed. Downgraded TypeScript from `^7.0.2` to `~6.0.2` after `typescript-eslint` rejected the newer version's peer range.
- Files: `package.json`, `eslint.config.mjs`, `.prettierrc`, `.prettierignore`, `.husky/*`, `scripts/*.sh`, `CHANGELOG.md`.

### GLA-3 — Setup CLAUDE.md + AI workflow conventions

- Prompt: Write `CLAUDE.md` documenting stack, folder structure, and conventions for AI-assisted coding, informed by the same reference repos and by another project's memory of a similar Jira-driven, multi-repo workflow; then extend it with `.claude/` skills (`commit-gla`, `pr-gla`), commands (`/validate`, `/release`, `/test-branch`), instructions files ported from the reference repo's Nest-specific originals, ADR scaffolding, and a `PRODUCT.md` capturing the actual assignment brief once it was shared.
- Key decisions: chosen architecture is MVC + repository layering (`routes → controllers → services → repositories → Prisma`), explicitly not Nest-style modules — the user corrected an early draft that framed this as a comparison to Nest, twice, until the doc stated the architecture as this project's own decision with no comparison language at all. Zod and Prisma are documented as "coming soon" and were deliberately not installed ahead of the ticket that needs them, per explicit user choice. After GLA-2's PRs merged, the user explicitly rejected deleting the now-merged branches — branches are kept unless told otherwise.
- Files: `CLAUDE.md`, `.claude/skills/*`, `.claude/commands/*`, `.claude/instructions/*`, `docs/adr/*`, `PRODUCT.md`, `CHANGELOG.md`.

### GLA-5 — Initialize backend repo (Express/Nest, Prisma, Supabase)

- Prompt: Implement GLA-5 (scaffold Prisma + Supabase Postgres, Supabase Auth client, zod, layered folder structure, health check at `GET /api/v1/health`) using the project's git-worktree workflow.
- Key decisions: the ticket named Prisma, zod, and Supabase Auth — none installed yet — so each was confirmed individually rather than assumed: Prisma scaffolded without a live connection (no credentials exist), zod and Supabase Auth left as "coming soon" (no request body or auth flow to build against yet). `pnpm add -D prisma` resolved to the `8.0.0-rc.12` prerelease by default; pinned to the latest stable `7.10.0` to match `@prisma/client` instead. Prisma 7 dropped the schema-embedded `datasource.url` in favor of `prisma.config.ts` + a `@prisma/adapter-pg` driver adapter — adopted that pattern, plus a Supabase-specific two-connection-string split (`DATABASE_URL` for runtime via the transaction pooler, `DATABASE_SESSION_POOLER_URL` for migrations via the session pooler, since transaction pooling doesn't support the session-level operations `prisma migrate` needs). Generated Prisma client output was placed at `prisma/generated/` (not `src/generated/`) per explicit correction; since that generated TypeScript source lives outside `src/`, `tsconfig.json`'s `rootDir` moved from `src` to the repo root and the build output shifted from `dist/index.js` to `dist/src/index.js` (`package.json` `main`/`start` updated to match). The existing inline `GET /health` route was moved into the `routes → controllers` layering and re-pathed to `/api/v1/health` per the ticket's required API surface, returning the repo's `{ data }` envelope instead of a bare object.
- Files: `package.json`, `pnpm-lock.yaml`, `tsconfig.json`, `prisma.config.ts`, `prisma/schema.prisma`, `.gitignore`, `.env.example`, `src/lib/prisma.ts`, `src/routes/health.routes.ts`, `src/controllers/health.controller.ts`, `src/index.ts`, `CLAUDE.md`, `README.md`, `CHANGELOG.md`.

### GLA-9 — Define Prisma schema (LoanScheme model) + seed data

- Prompt: Using the GLA worktree workflow, implement GLA-9 (define the `LoanScheme` Prisma model, migration, and seed script with at least 2 plans), consulting the reference production repo (`comverse_backend`) for Prisma model/seed conventions rather than inventing them. Mid-task, redirected twice: first to restructure the schema into one file per resource (matching the reference repo's `schema/<resource>.prisma` split) instead of a single `schema.prisma`, then to relocate all of Prisma (schema, migrations, generated client, seed script) from the repo root into `src/prisma/`, again matching the reference repo's layout.
- Key decisions: the ticket listed only 4 fields (`id`, `name`, `interestRate`, `maxLtv`, `createdAt`); asked the user whether to also apply this repo's own documented-but-unapplied reference-data conventions (`isActive` toggle, snake_case `@map`/`@@map`) — user chose to apply them. `interestRate`/`maxLtv` typed as `Decimal(5,2)` (percentages, not currency, but still exact-precision, matching the reference repo's `discountPercentage` field) rather than `Float`. Seed script (`src/prisma/seed.ts`) connects via its own `PrismaPg` adapter against `DATABASE_SESSION_POOLER_URL` — not the runtime `DATABASE_URL` used by `src/lib/prisma.ts` — mirroring the reference repo's seed/migration convention rather than the app's own runtime connection, since Supabase's transaction pooler doesn't reliably support the same operations. Moving Prisma under `src/prisma/` did **not** let `tsconfig.json`'s `rootDir` revert to `src` as its doc comment previously implied it would once generated output moved inside `src/` — `prisma.config.ts` itself has to stay at the repo root (Prisma 7 CLI convention) and is also typechecked, so `rootDir` stays at the repo root and `dist/src/index.js` is unchanged; corrected that reasoning in `CLAUDE.md` rather than leaving the stale justification in place. Migration and seed were run and verified against the live Supabase project (real credentials already present in `.env`, unlike when GLA-5 scaffolded Prisma with none) — confirmed both rows queryable via `DATABASE_URL` directly, not just via `prisma db seed`'s own connection.
- Files: `src/prisma/schema/main.prisma`, `src/prisma/schema/loan-scheme.prisma`, `src/prisma/schema/migrations/20260830103213_init_loan_scheme/`, `src/prisma/seed.ts`, `src/lib/prisma.ts`, `prisma.config.ts`, `package.json`, `tsconfig.json`, `.gitignore`, `.env.example`, `CLAUDE.md`, `README.md`, `CHANGELOG.md`.

### GLA-11 — POST /api/v1/leads/submit

- Prompt: "Work on the story GLA-11 using the workflow and the worktree." The Jira ticket body (fetched via the Atlassian MCP) specified Zod validation, purity/LTV calculation, 7-day mobile-number deduplication, and Prisma `Lead` persistence as one unit, explicitly covering `GLA-12`–`GLA-15`.
- Key decisions: installed Zod now that a ticket actually needs it (previously documented as "coming soon"), using its v4 top-level APIs (`z.uuid()`, array-form `z.literal([18, 22, 24])`) rather than the older chained equivalents. The ticket grouped `netWeightGrams <= grossWeightGrams` under "Zod validation," but this repo's own `validation.md` classifies cross-field conditions as business logic — implemented it as a service-layer guard (`ensureNetWeightNotExceedingGross`, 400) instead of a Zod `.refine()`, and likewise checked `selectedPlanId` against the database in the service (`ensureLoanSchemeExists`, 404) rather than the schema. `purityKarat` modeled as a real Prisma enum (`GoldPurity`) per this repo's "fixed set of values → Prisma enum" rule, with a pure `toGoldPurity` mapper in `lead.util.ts` converting the validated `18 | 22 | 24` literal for persistence — the pure gold weight formula itself still multiplies by the raw karat number, not the enum. `MAX_LTV_RATIO` (0.75) and `GOLD_RATE_PER_GRAM` (a placeholder `6500`, no live rate feed exists) are hardcoded constants in `src/config/gold-loan.config.ts`, matching the ticket's literal formula and the assignment brief's framing of 75% as a single global RBI ceiling — deliberately not read from `LoanScheme.maxLtv`, even though that field exists and is currently `75.0` for both seeded plans, since the ticket didn't ask for per-scheme LTV differentiation; worth revisiting if that becomes a real requirement. Added a new `ValidationError` (400, carries field-level `details`) alongside `DuplicateLeadError` (409) and `LoanSchemeNotFoundError` (404), all following the existing `AppError` subclass convention; the error-handler middleware checks `ValidationError` before the generic `AppError` branch so its `details` array reaches the client. Dedup window computed with `setUTCDate`, per this repo's UTC-only date-math rule, even though `Lead.createdAt` is the first timestamptz field that rule has applied to in practice. Verified the exact assignment example (45g net @ 22K) against the live Supabase database end-to-end via `curl` — `pureGoldWeight` `41.25`, `maxEligibleLoan` `201093.75` — plus all four error paths (dedup, net>gross, bad mobile regex, unknown plan), then deleted the test row afterward.
- Files: `src/prisma/schema/lead.prisma`, `src/prisma/schema/loan-scheme.prisma`, `src/prisma/schema/migrations/20260830111549_add_lead_model/`, `src/config/gold-loan.config.ts`, `src/schemas/lead.schema.ts`, `src/middlewares/validate.middleware.ts`, `src/middlewares/error-handler.middleware.ts`, `src/lib/errors/*.ts`, `src/repositories/lead.repository.ts`, `src/repositories/loan-scheme.repository.ts`, `src/services/lead.service.ts`, `src/services/lead.util.ts`, `src/services/loan-scheme.service.ts`, `src/controllers/lead.controller.ts`, `src/routes/lead.routes.ts`, `src/types/lead.types.ts`, `src/index.ts`, `package.json`, `pnpm-lock.yaml`, `CHANGELOG.md`.

### GLA-16 — GET /api/v1/leads

- Prompt: "continue working on the next story that is GLA-16." The Jira ticket body specified fetching all `Lead` rows ordered by `createdAt` desc, masking `mobileNumber` in the response (e.g. `9876XXXX10`), and including the calculated loan amount and selected plan per record — explicitly covering `GLA-17`.
- Key decisions: primary checkout was free (on `dev`, clean — no other ticket's branch checked out), so branched directly there instead of spinning up a worktree, per this project's "worktrees are opt-in, not routine" rule. `maskMobileNumber` implemented as a pure function in `lead.util.ts` (first 4 + `XXXX` + last 2), matching the ticket's example exactly. The nested `selectedPlan` relation is fetched via `prisma.lead.findMany({ include: { selectedPlan: true } })` and returned as `plan: { id, name }` per this repo's response-shape rule (nested object for a related entity, never flattened `*Id`/`*Name` pairs). Trimmed the response to only fields the documented admin/partner dashboard (`PRODUCT.md` → Frontend Flow) actually uses — `id`, `customerName`, masked `mobileNumber`, `netWeightGrams`, `status`, `maxEligibleLoan`, `plan`, `createdAt` — deliberately leaving out `grossWeightGrams`/`purityKarat`/`pureGoldWeight`, which the ticket didn't ask for and no consumer currently needs, per this repo's "every response field must justify its existence" rule. Verified against the live database end-to-end (masked output `9123XXXX89` for input `9123456789`, correct nested plan and `maxEligibleLoan`), then deleted the test row.
- Files: `src/repositories/lead.repository.ts`, `src/services/lead.service.ts`, `src/services/lead.util.ts`, `src/controllers/lead.controller.ts`, `src/routes/lead.routes.ts`, `src/types/lead.types.ts`, `CHANGELOG.md`.

### GLA-21 — POST /api/v1/leads/calculate

- Prompt: "Work on GLA-21" — the Jira ticket body (Step 2: Dynamic Loan Calculator & Scheme Selection, explicitly covering GLA-22 + GLA-23) called for a backend preview endpoint the frontend calls as gross/net/purity inputs change, computing pure gold weight and max eligible loan server-side so the frontend never duplicates the financial math — plus the frontend consuming `GET /api/v1/loan-schemes` for plan-selection cards, which already existed from GLA-8.
- Key decisions: reused `calculatePureGoldWeight`/`calculateMaxEligibleLoan` from `lead.util.ts` and the same `GOLD_RATE_PER_GRAM`/`MAX_LTV_RATIO` constants `createLead` already uses, rather than introducing parallel logic — the ticket's "single source of truth for financial math" AC applies as much within this repo as to the frontend. `calculateLeadSchema` factors the three shared fields (`grossWeightGrams`, `netWeightGrams`, `purityKarat`) out of `createLeadSchema` into a shared object literal instead of duplicating them, so the two schemas can't drift. `selectedPlanId` deliberately excluded from the calculate payload: `maxEligibleLoan` is derived from the fixed 75% `MAX_LTV_RATIO` config constant, not `LoanScheme.maxLtv`, so plan selection doesn't affect the calculation (confirmed by reading `createLead`'s existing logic before assuming). Since the endpoint does no I/O (no DB read/write — it's a stateless computation validated and returned directly), both `leadService.calculateLead` and the controller's `calculateLead` are plain synchronous functions rather than `async`, unlike every other function in these files; Express 5 still forwards a synchronous throw (e.g. the net-vs-gross `ValidationError`) to the error middleware without needing an `async`/`await` wrapper. Placed `calculateLead` under `// READS` in the service (non-mutating, no different from a computed get) but under `// WRITES` in the controller/route (this repo's controller convention keys off HTTP method, and it's a `POST`). Verified end-to-end via `curl` against the assignment's worked example (45g net @ 22K → `pureGoldWeight: 41.25`, `maxEligibleLoan: 201093.75`), plus the net>gross and bad-purity error paths, and confirmed `GET /api/v1/loan-schemes` still returns the two seeded plans unchanged.
- Files: `src/schemas/lead.schema.ts`, `src/types/lead.types.ts`, `src/services/lead.service.ts`, `src/controllers/lead.controller.ts`, `src/routes/lead.routes.ts`, `CHANGELOG.md`.
