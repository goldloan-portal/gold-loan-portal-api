PORT=4000

# Prisma / Supabase Postgres — see CLAUDE.md > Stack > Database.
DATABASE_URL=                    # runtime (src/lib/prisma.ts adapter) — Supabase transaction pooler
DATABASE_SESSION_POOLER_URL=     # prisma.config.ts datasource + src/prisma/seed.ts — migrations & seeding, Supabase session pooler

# Supabase Auth — not wired up yet (see CLAUDE.md > Coming soon).
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
