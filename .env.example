PORT=4000

# Prisma / Supabase Postgres — no live connection yet (see CLAUDE.md > Coming soon).
DATABASE_URL=                    # runtime (src/lib/prisma.ts adapter) — Supabase transaction pooler
DATABASE_SESSION_POOLER_URL=     # prisma.config.ts datasource — migrations only, Supabase session pooler

# Supabase Auth — not wired up yet (see CLAUDE.md > Coming soon).
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
