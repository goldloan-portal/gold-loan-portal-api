// Placeholder rate until a live gold-rate feed is wired up — update here, not inline at call sites.
export const GOLD_RATE_PER_GRAM = 6500;

// RBI compliance ceiling for gold loan NBFCs — not a per-scheme business choice.
export const MAX_LTV_RATIO = 0.75;
